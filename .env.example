# Server
NODE_ENV=development
PORT=4000
CLIENT_URL=http://localhost:5173
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/healthcare_appointment_manager?schema=public

# Client
VITE_API_URL=http://localhost:4000/api

# Future milestones only. Replace locally when those integrations are implemented.
JWT_SECRET=replace-with-a-long-random-development-secret
LLM_PROVIDER=mock
LLM_API_KEY=
LLM_MODEL=gpt-4o-mini
EMAIL_PROVIDER=mock
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=replace-locally
SMTP_PASS=replace-locally
SMTP_FROM="Healthcare App <no-reply@example.com>"
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=
GOOGLE_REFRESH_TOKEN=
GOOGLE_CALENDAR_ID=
